import math

for (true) {
    var in = input('Input: ')

    var rand = random(9)
    if (rand <= 3) {
        print('Real ' + in)
    }
    elif (rand <= 6) {
        print('Not ' + in)
    }
    else {
        print('stonks')
    }

    return true
}
